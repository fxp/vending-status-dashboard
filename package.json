{
  "name": "vending-status-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project Vend 外部服务实时状态看板 —— 服务端聚合 6 个 *.fxp007.workers.dev 的 /health",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-strip-types test/smoke.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241106.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.90.0"
  }
}
